# Tasks

- [ ] Update `App.tsx` to store and pass `originalUpstreams` and `originalLocations` @frontend
- [ ] Update `ConfigEditor` props to accept original config values @frontend
- [ ] Implement `ReadOnlyRawConfig` capability: Switch Monaco Editor to ReadOnly mode in `ConfigEditor` @frontend
- [ ] Implement `DiffVisualizer` capability: Replace standard Editor with DiffEditor in `ConfigEditor` @frontend
- [ ] Verify that wizard changes are reflected in the diff view @verification
